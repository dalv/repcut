{
  "name": "repcut-rep-detector-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser prototype for RepCut auto rep detection (Phase 1: pause-based segmentation via frame-difference motion energy). Validates the algorithm on real acro videos before porting to Swift.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
